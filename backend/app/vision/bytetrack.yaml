# ByteTrack tuned for drone footage: lower thresholds to keep weak detections,
# and a long track buffer so IDs survive tree/building occlusions.
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.25
track_buffer: 60
match_thresh: 0.85
fuse_score: true
